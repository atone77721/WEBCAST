name: 🏈🏒⚾ Update NBAWebcast Playlist 📺

on:
  schedule:
    - cron: '0 * * * *'  # runs every hour
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install Python dependencies & Playwright
        run: |
          python -m pip install --upgrade pip
          pip install playwright urllib3 aiohttp beautifulsoup4 lxml
          playwright install-deps
          playwright install

      - name: 🔒 Restore Base64 Script (silent)
        env:
          NBAWEBCAST_B64: ${{ secrets.NBAWEBCAST_B64 }}
        shell: bash
        run: |
          echo "⚙️ Restoring script..."
          echo "$NBAWEBCAST_B64" | base64 --decode > nbawebcast.py 2>/dev/null
          echo "✅ nbawebcast.py restored."

      - name: 🎯 Run scraping script (errors only)
        shell: bash
        run: |
          echo "🚀 Running nbawebcast.py (errors only)..."
          LOG_FILE=$(mktemp)

          # Run script quietly
          if ! python webcast.py >"$LOG_FILE" 2>&1; then
            echo "::error::❌ nbawebcast.py failed — see log below:"
            cat "$LOG_FILE"
            rm -f "$LOG_FILE"
            exit 1
          fi

          rm -f "$LOG_FILE"
          echo "✅ nbawebcast.py completed successfully"

      - name: 💾 Commit & Safely Push if Playlists Changed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add NBAWebcast.m3u8

          if git diff --cached --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi

          git commit -m "🔁 Update playlists $(date -u +'%a %b %d %T UTC %Y')"

          sleep $((RANDOM % 10 + 5))

          if ! git push origin main; then
            echo "⚠️ Push rejected. Trying safe fetch + merge..."
            git pull origin main --rebase || {
              echo "❌ Rebase failed. Exiting to avoid corruption."
              exit 1
            }
            git push origin main || {
              echo "❌ Second push failed. Remote changed again."
              exit 1
            }
          fi
